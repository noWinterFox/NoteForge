using MusicTheoryLib.Data.Enums;

namespace MusicTheoryLib.Classes.Generation;

public class ScaleGenerator
{
    public Note[] GenerateScale(string key, string mode)
    {
        Note[] scaleNotes = new Note[7];
        
        throw new ArgumentException("Something went wrong");
    }
}